redis存储时，有序列化策略

spring-data-redis默认采用的序列化策略有两种，一种是String的序列化策略，一种是JDK的序列化策略。

-----------------------------------------------------------------

redis命令

key:
	del、expire、ttl、persist、type、exists

string:
	set|get、
	strlen、
	append、incr|decr

hash:
	hvals、hset|hget|hdel、
	hlen、
	hexists、hkeys、
	hscan

list:
	lrange、lpop|lpush|rpop|rpush、
	llen、
	lindex

set:
	smembers、sadd|srem、
	scard、
	sdiff|sinter|sunion、sismenmber、
	scan

sorted set:
	zrange、zadd|zrem|zscore
	zcard
	zcount、zincrby、zrank