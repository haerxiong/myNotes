与关系数据库对比：
	Index（数据库） > Type（表） > Document（记录）

特色功能：
	全文搜索（相关性、高亮）
	聚合（统计功能强大）
	等等。。。

原理：
	Index只是ES的逻辑命名空间，实际指向多个“分片”。

	默认情况下，一个Index被分配5个主分片，“复制分片”属于副本。

	集群间会迁移分片，保持集群平衡。

	主分片在Index创建时数量就确定了，因此我们通过增加“复制分片”的数量来提高集群的工作吞吐量。