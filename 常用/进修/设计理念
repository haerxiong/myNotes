各种类型的对象VO DTO DO PO

	PO，持久化对象。与数据库对应，包括关系表。
	DO，领域对象。与现实业务实体对应。
	DTO，数据传输对象。表现层与服务处间传输数据，屏蔽两侧多余数据。
	VO，视图对象。把逻辑语言转换为展示内容，如1、0转为化有效、无效。

	PO与DO大致相同，DTO与VO大致相同。
	除了Do，其他大部分是简单的POJO。


领域驱动设计方式---充血模型
	
	领域模型：除了自身的属性、状态之外，他有属于自己职责范围之内的行为，这些行为封装了属于其领域内的业务逻辑。